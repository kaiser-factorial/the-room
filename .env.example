# Copy to .env and fill in. Real .env is gitignored — never commit keys.

# Required for real sessions (agents + summarizer)
OPENROUTER_API_KEY=

# Live sink + control plane (viewer feed, admin commands). Service role key:
# Supabase dashboard → catchall → project settings → API keys → service_role
SUPABASE_URL=https://wfrxfhpiuxofmfdjpuvv.supabase.co
SUPABASE_SERVICE_KEY=

# Optional dry-run / overrides
# ROOM_STUB=1
# ROOM_CONDITION=house
# ROOM_MINUTES=30
