{
  "description": "F4½ full tool bench: zero-cost websearch (alongside), a shared filesystem everyone reads AND can execute from ([WRITE: name]…[/WRITE], room-public — build shared/project.py together), and a pyodide python sandbox ([RUN]…[/RUN]) whose code and output are spoken to the room (runPublic — pair-programming mode). One tool action per seat per turn; journal off, as in control.",
  "search": {
    "enabled": true,
    "mode": "alongside",
    "gated": false,
    "notice": true,
    "maxResults": 5
  },
  "tools": {
    "files": true,
    "python": true,
    "budget": "per-seat",
    "notice": true,
    "pythonTimeoutSeconds": 30,
    "pythonPackages": [
      "numpy",
      "pandas",
      "sympy",
      "networkx",
      "matplotlib"
    ],
    "pythonInstall": true,
    "runPublic": true
  }
}
